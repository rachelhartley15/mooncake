{
  "name": "mooncake-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo workspace for Decentralized AI (DeAI) engineering projects.",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev:reporter": "pnpm --filter 01-intel-reporter dev",
    "build:all": "pnpm -r build"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}